# Ryze UI Generator - Environment Variables

# API Key — supports OpenAI (sk-...) or OpenRouter (sk-or-...)
OPENAI_API_KEY=your_api_key_here

# Optional: Model name (defaults to gpt-4o)
OPENAI_MODEL=gpt-4o

# Optional: Custom base URL (auto-detected for OpenRouter keys)
# OPENAI_BASE_URL=https://openrouter.ai/api/v1
